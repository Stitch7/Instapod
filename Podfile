platform :ios, '9.0'
use_frameworks!

target 'Instapod' do
    pod 'AEXML', '~> 2.1.0'
    pod 'PKHUD'

    target 'InstapodTests' do
        inherit! :search_paths
    end

    target 'InstapodUITests' do
        inherit! :search_paths
    end
end
